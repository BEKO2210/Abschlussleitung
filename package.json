{
  "name": "abschlusszeitung-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter=spec tests/"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
